name: Documentation

on:
  push:
    branches:
      - master
      - actions

jobs:
  docs:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      uses: actions/setup-node@v1
      with:
        node-version: 12
    - name: Build documentation
      run: |
        npm install
        npm docs
